# ===========================================
# Single env example – use for Docker AND local
# ===========================================
# DOCKER:  cp .env.example .env   then  docker-compose up -d
# LOCAL:   cp .env.example .env.development  (or .env.production / .env.test)
#          Then set MYSQL_HOST=localhost, REDIS_HOST=localhost, DATABASE_URL (see below)
# ===========================================

# Server Configuration
PORT=8000
SERVER_URL=http://localhost:8000
NODE_ENV=development

# Database Configuration
# Prisma uses DATABASE_URL only (no variable expansion in .env).
# Use full URL: mysql://USER:PASSWORD@HOST:3306/DATABASE
# 
# DOCKER: MYSQL_HOST=mysql (service name)
# LOCAL: MYSQL_HOST=localhost
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=backend_nodejs_dev
# DOCKER: mysql://app_user:app_password@mysql:3306/backend_nodejs_dev
# LOCAL: mysql://root:@localhost:3306/backend_nodejs_dev
DATABASE_URL=mysql://root:@localhost:3306/backend_nodejs_dev

# Redis Configuration
# DOCKER: REDIS_HOST=redis (service name)
# LOCAL: REDIS_HOST=localhost
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Frontend URLs (comma-separated for CORS)
FRONTEND_URL=http://localhost:3000,http://127.0.0.1:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# JWT Configuration
JWT_ACCESS_TOKEN_SECRET=your-super-secret-access-token-key-change-in-production
JWT_REFRESH_TOKEN_SECRET=your-super-secret-refresh-token-key-change-in-production
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=7d
